buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:9.2.0' // Совместимо с Gradle 8.2
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}