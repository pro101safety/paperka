buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.2' // Совместимо с Gradle 8.2
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}