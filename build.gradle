buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:9.3.1' // Совместимо с Gradle 8.2
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}